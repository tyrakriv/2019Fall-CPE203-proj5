import processing.core.PImage;

import java.util.List;

public class Down extends Entity{

    public Down(String id, Point position,
                    List<PImage> images) {
        super(id, position, images);
    }
}
